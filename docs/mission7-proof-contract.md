# Mission 7 proof contract

A final V1 proof is valid only when it is bound to the production Git commit, all Mission 0–6 proof files pass, the identity/security/money snapshots pass, the controlled operating cycle has zero core-route 5xx responses, and the Architect certification record stores the same commit and evidence digest.
