# Architect signing evidence

The final record stores the signing profile, signed timestamp, production deployment ID, commit SHA, gate counts, evidence bundle, and a SHA-256 integrity digest. The digest proves record integrity; it is not an external certificate-authority signature.
