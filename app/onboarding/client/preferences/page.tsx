import { ClientOnboardingWorkspace } from "@/components/onboarding/client-onboarding-workspace";

export default function ClientPreferencesOnboardingPage() {
  return <ClientOnboardingWorkspace step="client_preferences" />;
}

