import Link from "next/link";
import type { Route } from "next";
import { ArrowLeft, ArrowRight, Sparkles } from "lucide-react";
import { Card } from "@/components/ui/card";
import { getEngagementProvider } from "@/lib/engagement/provider";
import { buildMarketplaceLeaderboards } from "@/lib/marketplace/growth";
import { getMarketplaceProvider } from "@/lib/marketplace/provider";
import { getTrustProvider } from "@/lib/trust/provider";

export default async function DiscoveryStylesPage() { const [marketplaceProvider, engagementProvider, trustProvider] = await Promise.all([getMarketplaceProvider(), getEngagementProvider(), getTrustProvider()]); const [runtime, engagementState, trustState] = await Promise.all([marketplaceProvider.readRuntime(), engagementProvider.readState(), trustProvider.readState()]); const leaderboards = buildMarketplaceLeaderboards(runtime, engagementState, trustState); return <main className="mx-auto max-w-7xl px-4 py-4 sm:px-6 lg:px-10 lg:py-8"><Card className="rounded-[36px] p-6 sm:p-8"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="surface-label text-[#d7ffab]">Style discovery</p><h1 className="mt-4 text-balance text-4xl font-semibold sm:text-5xl" data-display="true">Browse the marketplace by haircut style</h1><p className="mt-4 max-w-3xl text-sm leading-7 text-white/64">Turn discovery into intent through style tags, trending demand, and barbers who already prove they can deliver the look.</p></div><div className="flex flex-wrap gap-3"><Link href="/discover" className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-black/25 px-5 py-3 text-[11px] font-semibold uppercase tracking-[0.22em] text-white transition hover:border-[#7CFF00]/24 hover:text-[#d7ffab]"><ArrowLeft className="h-4 w-4" />Back to discover</Link><Link href="/leaderboards" className="inline-flex items-center gap-2 rounded-full border border-[#cfff93]/40 bg-[linear-gradient(135deg,#7cff00_0%,#b7ff58_100%)] px-5 py-3 text-[11px] font-semibold uppercase tracking-[0.22em] text-black shadow-[0_14px_34px_rgba(124,255,0,0.24)] transition hover:translate-y-[-1px] hover:shadow-[0_18px_38px_rgba(124,255,0,0.28)]"><Sparkles className="h-4 w-4" />See leaderboards</Link></div></div></Card><section className="mt-4 grid gap-4 lg:grid-cols-2">{leaderboards.styleLeaders.map((style) => <Card key={style.styleTag.id} className="rounded-[32px] p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="surface-label">Trending style</p><h2 className="mt-3 text-2xl font-semibold" data-display="true">{style.styleTag.name}</h2></div><span className="status-pill text-[#d7ffab]">{style.trendLabel}</span></div><p className="mt-3 text-sm text-white/60">{style.regionLabel}</p><div className="mt-4 space-y-3">{style.barbers.map((entry) => <div key={`${style.styleTag.id}-${entry.barberId}`} className="rounded-[22px] border border-white/8 bg-black/20 p-4 transition hover:border-[#7CFF00]/18 hover:bg-black/30"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-medium">{entry.barberName}</p><p className="mt-2 text-sm text-white/58">{entry.supportingText}</p></div><span className="status-pill text-[#d7ffab]">{entry.scoreLabel}</span></div><div className="mt-4 flex flex-wrap gap-3"><Link href={`/barber/${entry.username}` as Route} className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-black/25 px-4 py-2 text-[11px] font-semibold uppercase tracking-[0.22em] text-white transition hover:border-[#7CFF00]/24 hover:text-[#d7ffab]">View profile</Link><Link href={entry.bookingHref} className="inline-flex items-center gap-2 rounded-full border border-[#cfff93]/40 bg-[linear-gradient(135deg,#7cff00_0%,#b7ff58_100%)] px-4 py-2 text-[11px] font-semibold uppercase tracking-[0.22em] text-black">Book from style <ArrowRight className="h-4 w-4" /></Link></div></div>)}</div></Card>)}</section></main>; }

